# Benchmark setup

The earlier experiment-setup form has been replaced by [Benchmark setup](benchmark_setup.md).
Benchmarks have a title, optional description, automatically generated versions
and task IDs, and optional task prompts. Create experiments beneath a benchmark
and use their IDs in the harness. The authoring UI requires no approval form.
